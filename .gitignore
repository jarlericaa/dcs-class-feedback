# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# env — never commit secrets
.env
.env.local
.env.*.local

# build / misc
*.tsbuildinfo
coverage/

# db
postgres-data/

# Visual QA references. Kept on disk for inspection, never committed.
docs/ui-references/

# agent tooling — installed per machine, never committed
.agents/
.claude/skills/
.claude/settings.local.json
.codex/
.impeccable/
.ccmux/
.pi-subagents/
agent/
skills-lock.json
.mcp.json

# QA run output — regenerate with the scripts in scripts/qa
docs/qa/

# Output of a verification build run while the dev server holds .next
.next-verify
